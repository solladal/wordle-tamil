# Production API base URL - committed to the repo since it's a public URL (not a secret).
# Used automatically by `npm run build` / `npm run deploy` (GitHub Pages).
REACT_APP_WORDS_API_BASE=https://wordle-tamil-words-api.muralikumar-s92.workers.dev
